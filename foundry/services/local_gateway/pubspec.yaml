name: prototype_local_gateway
description: Local-only provider gateway for Prototype Foundry.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  prototype_gateway_protocol:
    path: ../../packages/prototype_gateway_protocol

dev_dependencies:
  test: ^1.25.8
