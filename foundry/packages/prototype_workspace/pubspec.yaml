name: prototype_workspace
description: Pure-Dart project, revision and review domain for Prototype Forge.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dev_dependencies:
  test: ^1.25.8
