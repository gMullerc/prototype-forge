name: prototype_material_catalog
description: Material fixture catalog with realistic components for Prototype Foundry.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  prototype_flutter:
    path: ../prototype_flutter
  prototype_runtime:
    path: ../prototype_runtime
  prototype_spec:
    path: ../prototype_spec

dev_dependencies:
  flutter_test:
    sdk: flutter
