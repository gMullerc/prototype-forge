name: prototype_export
description: Provider-neutral export contracts for validated Prototype Specs.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  prototype_spec:
    path: ../prototype_spec

dev_dependencies:
  test: ^1.25.8
