# Third-party notices

The initial Prototype Foundry implementation was written independently and does not copy source code from Flutter GenUI, A2UI renderers, or the previous Prototype Studio MVP.

The architecture is informed by publicly documented declarative UI concepts. Any future source import must be recorded here with its upstream revision and license before it is merged.

OpenCode is invoked as an independently installed local executable through its
documented HTTP API. OpenCode source or binaries are not copied or bundled in
this repository.
